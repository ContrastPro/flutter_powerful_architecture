import 'package:flutter/material.dart';

class TicketsPage extends StatelessWidget {
  const TicketsPage({Key? key}) : super(key: key);

  static const routeName = '/tickets_pages/tickets';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
